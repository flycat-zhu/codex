# Python requirements for Codex CLI

# 核心库
pypdf==5.7.0
pandas==2.2.0
matplotlib==3.7.0
openpyxl==3.1.0

# skills/（docx、pptx、xlsx office 脚本 + pdf2image）
defusedxml==0.7.1
lxml==5.3.0
Pillow==10.4.0
pdf2image==1.17.0

# 文档处理
python-docx==1.2.0
reportlab==4.4.7
pypandoc==1.13  # Pandoc Python 接口（需要系统安装 pandoc）

# 服务器依赖
requests==2.31.0

# 科研可视化（与 skills 内 matplotlib / seaborn / plotly / statsmodels / scientific-visualization 配套；不固定小版本）
numpy
seaborn
statsmodels
plotly
